<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title><%= data.name %></title>
</head>
<body>
  <h2><%= data.name %>の編集</h2>

  <form action="/saitama/update/<%= id %>" method="POST">
    <table>
      <tr><th>項目</th><th>データ</th></tr>

      <tr>
        <td>店名</td>
        <td><input type="text" name="name" value="<%= data.name %>"></td>
      </tr>

      <tr>
        <td>住所</td>
        <td><input type="text" name="adress" value="<%= data.adress %>"></td>
      </tr>

      <tr>
        <td>営業時間</td>
        <td><input type="text" name="open" value="<%= data.open %>"></td>
      </tr>

      <tr>
        <td>行くのにかかる時間</td>
        <td><input type="number" name="time" value="<%= data.time %>"></td>
      </tr>

      <tr>
        <td>評価</td>
        <td><input type="text" name="comment" value="<%= data.comment %>"></td>
      </tr>
    </table>

    <input type="submit" value="登録">
  </form>

  <p><a href="/saitama">埼玉一覧に戻る</a></p>
</body>
</html>
